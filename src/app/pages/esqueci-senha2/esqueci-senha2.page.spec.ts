import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EsqueciSenha2Page } from './esqueci-senha2.page';

describe('EsqueciSenha2Page', () => {
  let component: EsqueciSenha2Page;
  let fixture: ComponentFixture<EsqueciSenha2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EsqueciSenha2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
